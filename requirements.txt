# Alpaca Clean-Sheet CAPI Option 2.5 — backend + cockpit dependencies.
# Paper-first beta: paper credentials live in .env (gitignored); no live-
# trading path exists anywhere in this codebase.

# Backend (the durable engine)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# Cockpit (thin Streamlit client) + its HTTP calls to the backend
streamlit>=1.30
requests>=2.31

# Tests (unit tests are IO-free; httpx drives the FastAPI TestClient)
pytest>=8.0
httpx>=0.27
anyio>=4.0

# Phase 4 — Alpaca paper adapter (optional at install time).
# This is the ONLY permitted Alpaca SDK for this project.
# Required only when BROKER_ADAPTER=alpaca (real paper integration with Alpaca).
# The import is lazy (inside AlpacaPaperAdapter.__init__), so the standard
# test suite and CI run without it — unit tests never touch the Alpaca network.
# Paper-only: the adapter only ever constructs a paper TradingClient; there is
# intentionally no live-trading path in this codebase.
alpaca-py>=0.20
