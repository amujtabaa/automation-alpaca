# Python
__pycache__/
*.py[cod]
*.so
.Python
env/
venv/
.venv/
ENV/
*.egg-info/
.eggs/

# Pytest temp basetemp (used on Windows when the default temp dir errors)
.pytest_tmp/
.pytest-tmp/
.pytest_cache/

# Coverage artifacts (measured in CI; not committed)
.coverage
.coverage.*
htmlcov/
coverage.xml

# SQLite database (created at runtime, not committed)
data/
*.db
*.sqlite
*.sqlite3

# Credentials / environment
.env
.env.local

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Claude Code local/personal files (not shared with the team)
CLAUDE.local.md
.claude/settings.local.json
