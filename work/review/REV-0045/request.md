---
type: Review Request
rev_id: REV-0045
title: "WO-0140 R6a-R — truth-model remediation: independent gate-clearing review"
requested_of: Codex (independent cross-model review seat)
implementer: "Claude (planning seat), by explicit operator seat swap (Ameen 2026-07-27)"
references: REV-0044 (ACCEPT-WITH-CHANGES; R-1/R-2 gating)
branch: codex/signal-r6a-rails-store
base_sha: b48235e
head_range: "a012bf2..HEAD (WO-0140 remediation commits)"
work_order: work/queue/WO-0140-r6a-truth-model-remediation.md (rev-3, operator-ratified, on the planning branch)
gate: "This packet clears the R6a gate REV-0044 left open. WO-0104a stays REVIEW until your verdict; R6b and D-2a stay blocked."
---

# REV-0045 — request for independent review (Codex seat)

**Why you own this gate:** the implementer is the REV-0044 author. Per the repo rule that no seat
reviews its own work, the seats swapped for WO-0140 (operator-confirmed, recorded in the WO
frontmatter and `work/active/SIGNAL-R6aR-STATE.md`): Claude implemented, you hold the gate. Your
verdict clears or blocks REV-0044's R-1/R-2 — an addendum under REV-0044 referencing this packet is
staged by you, not by the implementer.

**Authorized defensive scope.** This is authorized defensive assurance of a local stock-management
repository. There is no external target, network probing, credential access, persistence, or
offensive objective. Report security findings at the defect level — cause, impact, affected local
files, fix, and pass/fail evidence — without narrating reusable bypass procedures or exploit
payloads. Vocabulary: *paced-arrival accounting* · *budget-exhaustion accounting defect* ·
*refill-accounting defect* · *non-atomic transaction boundary* · *drift-repair semantics*.

## What changed (one paragraph)

REV-0044 found the delivered R6a made the event-log fold authoritative on live paths where the
ratified design said cache — bricking `initialize()` on any pre-R6a log (R-1) and putting O(log)
folds on the single-writer lock (R-2). WO-0140 restores the ratified boundary: the cache gates live
(incremental debit; the attributable path folds nothing), the log verifies at boundaries and
startup only (bounded release-exclusive folds, state-conditional seed, O(1) anchor), startup is
tolerant per producer (derived poisoned markers; a `6955208`-generated database OPENS), and
`release_producer` is the single human recovery, classified by log truth.

## Named review items — each needs its own explicit verdict

1. **The two pre-R6a corpora open** (fixtures generated by real `6955208` code, provenance in the
   JSON headers): exactly the offender poisoned, other producers unaffected, replay derives the
   identical poisoned set. `tests/test_signal_rails_remediation.py`.
2. **The debit path folds nothing** (scaling pin mirroring the delivered no-rescan pins; proven RED
   against the fold-per-rejection code). The boundary/release folds remain linear filtered scans —
   the **ratified scaling disclosure** — at most 2 per epoch plus the human release path.
3. **The state-conditional seed** (`row.seq − 1` when the epoch is open, because the row advances at
   opener time) with the two detonation shapes pinned (state-1 release; open-epoch restart) and the
   flat-seed mutant RED on both stores.
4. **The no-epoch (zero-width) release**: `epoch_start == released_at`, consuming the next epoch
   sequence **via the dedupe key** (the ratified payload list is unchanged — verify no field was
   added). Two implementer refinements over rev-3's literal text, both disclosed in the state file,
   need your faithful-or-drift ruling:
   - the release keys at **(log-derived floor)+1**, not (row)+1 — equal on every truthful state;
     against a lying-high row it regresses the carrier to log truth, trading the letter of the
     never-regress rule for a no-reminted-key guarantee;
   - the fold derives a zero-width heal's sequence by **parsing the release's own dedupe key**
     (`sequence_from_release_dedupe_key`, canonical-digits strict).
5. **The log-classified release (operator ruling "Option A", 2026-07-27)**: open log → state-1
   release from log truth; wedge/unfoldable → zero-width heal; **zero/interior → repair-and-refuse**
   (the repair COMMITS before the refusal raises). The interior/zero completion is an
   implementer-level refinement of the operator's ruling — verify it against F-H (no mid-cycle
   release event exists to launder a budget) and against replay/restart agreement (pinned).
6. **Read-structural, write-capped** (fold AND the SQLite row validator): logged/durable values above
   a ratified cap fold and serve; the caps bind in the builders. The standing rule is recorded in
   `pkl/` and `app/models.py`. The three re-homed cap pins and the re-aimed `overflow_bucket` case
   are the two authorized-list test edits — audit the closed list held.
7. **The `_atomic()` extension** (poisoned dict in both halves + the delivered drop-one pin) and the
   memory rollback pin's seam move to `_apply_incremental_budget_debit_unlocked`.
8. **Two refutation passes ran during implementation** (fresh-context agents with runnable probes;
   findings and fixes in the state file and commit messages). Their in-loop findings are defect
   filters, NOT independent review — your seat is the only independent review of this work.
9. **Spec/ADR amendments shipped in-change**: `02-lifecycle.md` release row and ADR-009's epoch-close
   bullet gain the no-epoch case. Per the one-packet reconciliation the operator ratified, these are
   review items HERE rather than a separate packet.
10. **Process disclosures you should weigh**: the git-checkout mutant-restoration defect (reverted
    uncommitted work, caught by the next regression run); a self-defeating mutant regex that ran a
    "check" against unmutated code (discarded, redone); a first heal-removal mutant that was only a
    syntax error (discarded, redone). All in the state file's evidence log.

## Evidence staged

- `work/active/SIGNAL-R6aR-STATE.md` — slice scoreboard, full evidence log, all disclosures.
- Commit-by-commit red-first history: RED pins at `a012bf2` precede every implementation commit.
- 189-test rails corpus green; `ruff` / `mypy` (77 files) / `lint-imports` (6 kept) green; the full
  CI-equivalent battery with branch coverage runs at request-staging time — its exact figures are in
  the state file's final entry (verify them yourself; do not take the implementer's word).

## What would BLOCK

Any live-vs-replay class-A divergence you can construct; a decisive pin you can turn inert; a
mutation from the recorded tables that stays green; an unratified payload field or vocabulary value;
a stop-condition breach (DDL/index/new setting/`server.py`/R6b surfaces); the closed test-edit list
exceeded; or completion evidence you cannot independently reproduce.
