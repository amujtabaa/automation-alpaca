# WO-0150 R1 remediation-01 focused recheck request

Review only the exact six-path candidate frozen by
`WO-0150-R1-IMPLEMENTATION-REMEDIATION-01-CANDIDATE-MANIFEST.md`, SHA-256
`72e2945cc4ecf2f1dfaea0e65f879b9bb02eb087bb79601866d5773babd75f19`,
over tracked parent `fdd99d9386994dc1910e891537fcc6cecc127434`.

The prior final acceptance result is retained negative evidence and recorded
exactly two P1 control gaps. The author has changed only:

1. `tests/execution_core/test_acquisition.py` to pin a literal successor
   identity/replay, isolated ordinal variation, and malformed-value refusal in
   all three commitment coordinates; and
2. `tests/execution_core/test_import_boundary.py` to scan venue-local
   correlation consumers with only the direct producer's return/allocation
   exemptions and to reject an in-module consumer mutant.

## Focused independent task

Re-derive the active R1 requirements from the work order and
`WO-0150-RED-CONTRACT-R1.md`. Hash every manifest path, inspect the two
remediations and their immediate production context, and run only the focused
acquisition/import test command through the repository virtual environment.

Determine whether each prior P1 is now failure-capable without adding E2
admission/currentness, registry mutation, late-fact behavior, persistence,
database/SQL, runtime, broker, credential, or network behavior. Do not repeat
unrelated broad review. If a new P0/P1 is directly exposed by the remediated
control or its immediate context, report it precisely; otherwise do not widen
the review.

Do not edit application or test code, commit, push, run SQL/DDL, initialize a
database, access a broker, or activate another work order. Write findings only
to `work/review/REV-0057/WO-0150-R1-IMPLEMENTATION-REMEDIATION-01-RECHECK-RESULT.md`.
State the manifest hash, verified path hashes, exact focused command/result,
P0/P1/P2 counts, any unverified gates, and a verdict. `ACCEPT` requires
P0=0/P1=0 and authorizes only the existing WO-0150 closeout sequence.
