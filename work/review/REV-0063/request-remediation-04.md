# REV-0063 - focused independent remediation-04 re-review request

Reviewer role: a fresh independent review context. Preserve every existing
`result*.md` artifact unchanged. Review the exact current head supplied by the
launch message and write findings only to
`work/review/REV-0063/result-remediation-04.md`. Do not edit the candidate,
the manifest, active work order, any request, the disposition, or earlier
result.

## Exact review target

Verify the current branch and supplied base/commit before review. Rehash every
manifest-covered path from exact bytes; independently hash the self-excluded
manifest and proposed ADR. Treat all earlier reviewer results as immutable
negative provenance, never acceptance evidence.

## Required disproof pass

1. Independently construct `broker-account-identity/v1` with the ADR framing,
   parts, text rules, and digest representation. Test that one valid selected
   provider account succeeds while a different account, account label/alias,
   missing/plural/non-string extraction, non-NFC text, case/trim/URL-decoded
   form, or substituted adapter extractor fails the declared comparison.
   Verify the raw external identifier is absent from the profile and all
   candidate/public-review artifacts.
2. Verify the changed `account_identity` contract preserves ADR-022's exact
   broker/environment/account/origin/credential fence at preflight and final
   claim, rejects mismatch without permitting mutation or later broker request,
   and does not create a second profile, provider, credential authority, or
   runtime/DDL work.
3. Recheck parser-free origin grammar, all three commitment framing rules,
   output exclusions, single mutation-eligible profile, market-source
   separation, capability-evidence sequencing, Alpaca Paper M2--M8, M1
   closure/M2 inactivity, terminal-review-only ratification routing, and every
   prohibition in the work order.

Use the original request severity/result format, including actual reviewed
hashes and unverified gates. `ACCEPT` is permitted only if P0=0 and P1=0. This
is an independent findings-only review, not permission to revise the candidate.
