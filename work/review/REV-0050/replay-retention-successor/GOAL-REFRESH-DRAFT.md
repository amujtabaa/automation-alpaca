## Objective

Complete authorized M1 as an independently reviewed, exact-head-CI-green foundation ready for M2
planning. Trust live code, Git, work orders, accepted ADR/PKL, reviews, evidence, and CI over memory.

## Live gate

At every resume, derive the gate from HEAD, status/diff, the active work order, latest review,
accepted decisions, and artifacts. Preserve accepted work/evidence. WO-0145–WO-0147 are
closed and WO-0148 is active unless the repository proves otherwise.

The current WO-0148 candidate closes non-last replay but is unacceptable while receipts grow with
history. Before more semantic implementation, require an exact human-ratified bounded decision and
matching WO/ADR/PKL scope. If absent, finish independent review to zero P0/P1 and return exact
approval text; never self-authorize. Once ratified, follow live records and advance.

The reviewed direction uses constant-size market state: generation/mode-bound strict coordinates,
derived identity, fail-closed invalidation with exact next-epoch fresh baseline, and separate
projection/market reducers. Preserve bounded cache and optional recorder—no lifetime receipts,
silent eviction, caller-shaped recovery, mandatory history store, or ignored accepted input. Another
design requires exact ratification and independent review.

## Working method

Before each authority change, RED freeze, implementation wave, candidate freeze, activation, and
handoff, run a critical counterexample pre-flight: reread authority/scope; inspect status/diff;
reconcile findings/hashes; challenge boundaries, restart, scaling, coordinates, composed
lifecycles, and failure ordering; select the smallest complete root correction.

Use RED-first development. Each material rule needs a focused control that fails for its intended
regression; use named mutation fail/restore evidence for critical semantics. Fix owning invariants.
Prefer simple, deterministic, typed, immutable, bounded solutions; avoid duplicated truth,
broad fallbacks, hidden authority, test-only paths, and symptom patches.

Keep semantic edits single-writer. Parallelize only useful bounded read-only work: Terra for
inventory/test mapping/record reconciliation; Sol for architecture, critical semantics, and
independent acceptance. After each material change, review, freeze, CI result, interruption, or
compaction, refresh a repository checkpoint with authority, candidate, status/diff, results,
findings, artifacts, and next gate. Re-anchor to the live tree after compaction.

## Gates and boundaries

Close WO-0148 only after ratified RED and implementation pass focused/stateful/scaling controls;
named mutations; predecessor, R2, execution-core, full-repository coverage, and
static/type/import/grammar/scope/governance checks; fresh independent exact-candidate ACCEPT with
zero P0/P1; reconciled closeout; and unchanged exact-head Python 3.11/3.12 CI. Failures and
remediable findings are progress, not goal blocks.

Then activate and complete WO-0149 under separately recorded authority. Prove WO-0145–WO-0149 as a
coherent non-squashed M1 foundation and produce the read-only branch-retirement manifest and
M2/master-landing handoff. Return READY TO BEGIN M2 only when every M1 work order is closed, both CI
versions are exact-head green, no accepted P0/P1 remains, and governance/evidence/handoffs reconcile.

Preserve paper-only safety and public contracts unless explicitly ratified. No credentials,
Alpaca/broker or external network activity, live/shadow trading, runtime wiring, persistent
application-database change, direct database tooling, M2 implementation, master merge, branch or
worktree deletion, or cleanup. Mock/disposable fixtures require recorded authority. Push only the
authorized branch for exact-head CI. Pause only for a genuine authority, ADR, scope, dependency,
repository-state, or prohibited-surface decision; otherwise continue.
