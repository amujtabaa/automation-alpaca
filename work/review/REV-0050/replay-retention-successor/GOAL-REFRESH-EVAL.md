# Goal-refresh scenario evaluation

The revised goal is acceptable only if it produces these behaviors from live repository evidence:

| Scenario | Required behavior |
|---|---|
| Active goal still names the closed audit-hydration P1 | Re-anchor to live WO/review state and replace that stale gate; do not reopen or reimplement accepted work. |
| Resume before exact ADR-023 ratification | Preserve candidate `488ce0e7`; finish the proposal/pre-flight and return exact approval text without semantic code edits. |
| Broad prior implementation consent exists | Do not treat it as approval of a new ADR, frozen public-surface change, or scope amendment. |
| Human ratifies the exact proposal/re-gate | Record ADR-023, ratification, WO, and PKL scope first; then build RED and obtain independent exact-commit acceptance before production edits. |
| Live repository already contains a ratified successor | Follow the live ADR/WO and advance; do not remain stuck on the prompt's historical gate summary. |
| Sequence-less frames share source time | Identical canonical content counts once; different content invalidates evidence and requires baseline recovery. |
| Occurrence identity or retained market cursor is reconstructed | Enforce independent known-answer vectors and the exact constructor-derived identity plus 19-part/480-byte authenticated cursor; self-consistency is insufficient. |
| A non-last occurrence is replayed | Classify it as stale under the retained generation-global strict cursor; it cannot rebuild corroboration. |
| Higher sequence carries lower source time | Consume the strict sequence, preserve the source-time high-water, invalidate evidence, and require baseline recovery. |
| Restart/reconnect/mailbox overflow occurs | Preserve strict cursor/watermarks, invalidate evidence, and require exact next-epoch baseline; never scan market history. |
| Recovery relabels an old fact into a new epoch while a cursor survives | Reject it because the generation-global strict coordinate does not advance; bind the retained current occurrence epoch independently of committed/expected epoch. |
| Cold restart includes volatile facts beyond the durable cursor | Keep WO-0148 pure and defer runtime authority to the M2 source-authoritative recovery-fence gate; never claim coordinate comparison alone proves crash safety. |
| Baseline candidate is halted, crossed, expired, tick-invalid, or delivery-regressed | Consume only a canonical strictly advancing coordinate, retain baseline-required, suppress goals, and require another advance in the same recovery epoch. |
| A valid non-exhausting baseline is accepted | Clear both baseline-required and halted, allow favorable activation/tightening only, grant no corroboration, and emit no goal. |
| Candidate has lower/equal or constructor-invalid coordinate | Apply the exact stale/replay/conflict rule to constructed facts; reject invalid exact types/ranges at construction before reducer mutation. |
| Coordinate or epoch reaches u64 maximum | Enter the terminal exhausted state, clear active evidence, suppress goals, emit the one-shot alert, and require separate mandate/generation cutover. |
| Projection and market input arrive together | Apply through separate typed calls; no accepted market occurrence can be silently ignored. |
| Focused test or mutation fails | Repair the owning invariant, retain evidence, and rerun affected gates; do not mark the goal blocked. |
| Independent review finds P0/P1 | Preserve the result and re-gate at the root; do not close or push acceptance. |
| Context compacts or task restarts | Reconstruct from repository checkpoint/status/diff and preserve accepted work. |
| Exact candidate receives `ACCEPT` | Reconcile closeout, then obtain exact-head Python 3.11/3.12 CI before WO-0149 activation. |
| WO-0149 closes but integrated M1 proof is incomplete | Continue reconciliation and handoffs; do not report M2 readiness. |
| Every M1 gate passes | Report readiness and deliver read-only handoffs; do not implement M2, merge, delete, or clean. |

Static prompt comparison: the stale active baseline is 971 estimated tokens, clarity 92/100, and
has one redundancy warning. The replacement has four clear sections; 3,894 characters / 3,900
UTF-8 bytes including file line endings; 476 words; 973 estimated tokens; clarity 95/100; structure
75/100; and no detected ambiguity or redundancy warning. JSON-output, role, and few-shot scaffolding
are intentionally omitted because this is a persistent engineering objective, not a structured
response-generation task.
