# WO-0148 ninth RED exact-commit review request

Status: **INDEPENDENT PRE-PRODUCTION TEST-CONTRACT REVIEW**

Review exact commit `706ed536790179fcb673aaedf96b3b728ee33d3c` against activation base
`d75806b1a79d1769db25ae962c0977cd9388a886`. This is the immutable ninth RED
candidate. Production `app/execution_core/protection.py` is deliberately absent and remains barred.

Read `AGENTS.md`, the `CLAUDE.md` safety core, the complete active WO-0148, its linked accepted
authority, the preserved eighth request/result/disposition chain, and the exact candidate diff.
Re-derive the contract independently from those sources. Do not inherit the implementation seat's
reasoning or treat the pre-freeze worktree verdict as exact-commit acceptance.

Hostile review objectives:

1. Attempt to disprove every normative WO clause and required mutation control represented by the
   RED contract, with special attention to the eighth-review replay fix and the complete static
   effect/call/import/binding-provenance model.
2. Try concrete transitive capability paths: callback donation, fake approved roots, rebinding,
   conditional or nested imports, arbitrary relative imports, implicit protocols, dynamic attribute
   access, and dynamic construction. Verify that the positive production-shaped skeleton remains
   implementable and that runtime no-output capture is only complementary evidence.
3. Require a concrete reachable bypass, counterexample, contract contradiction, or failure-capable
   missing control for P0/P1. Classify speculative hardening, style, and preference as P2/advisory.
4. Verify exact RED failure classification, failure-capable meta-oracles, predecessor preservation,
   static/format/grammar/diff/scope integrity, production absence, and all safety boundaries.

Author evidence to reproduce or independently reconcile:

- focused collection: 284 tests;
- exact RED result: 228 expected failures / 56 passes, comprising 225 protection-module-absence
  failures plus 3 required inventory/AST/export deltas, with no oracle-helper failure;
- predecessor execution-core corpus: 698/698 at the eighth candidate, unchanged by the ninth
  candidate's deliberate RED-only diff;
- Ruff check/format-check, Python 3.11 grammar parsing, diff check, activation-base scope check, and
  production-absence check: pass;
- current execution-core source effect scan: zero violations.

No `INV-*` entry is added or amended by this test-only candidate, so no new-invariant probe is due.
Do not implement production, edit tests or the WO, use credentials, call Alpaca, execute SQL/DDL,
initialize a database, alter runtime/persistence, or perform cleanup/deletion.

Write findings only to `work/review/REV-0050/RED-NINTH-RESULT.md`. For each finding include exact
file/line, authority, concrete disproof, and what resolves it. End with `BLOCK`,
`ACCEPT-WITH-CHANGES`, or `ACCEPT`; state unverified items and exact P0/P1 counts. This verdict
governs only permission to begin WO-0148 production implementation. It does not replace the later
implementation review or close the work order.
