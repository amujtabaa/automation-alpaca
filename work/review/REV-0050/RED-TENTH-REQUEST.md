# WO-0148 tenth RED exact-commit review request

Status: **INDEPENDENT PRE-PRODUCTION TEST-CONTRACT REVIEW**

Review exact commit `5c5bee9543b78fc2fa8f612c61d75d4fdbf52bae` against activation base
`d75806b1a79d1769db25ae962c0977cd9388a886`. This is the immutable successor RED candidate.
Production `app/execution_core/protection.py` is deliberately absent and remains barred.

Read `AGENTS.md`, the `CLAUDE.md` safety core, complete active WO-0148, linked accepted authority,
and the preserved eighth/ninth/Claude request-result-disposition chain. Re-derive the contract from
the exact candidate; do not inherit the author or pre-flight reviewers' reasoning. The prior
`RED-NINTH-RESULT.md` `BLOCK` remains authoritative for its own commit and was not rewritten.

Hostile objectives:

1. Attempt to disprove every normative WO clause and required mutation control represented by the
   complete deterministic, stateful, provenance, lifecycle, and import-boundary RED contract.
2. Re-attack changed-context replay across both market kinds and present/absent sequence, including
   full-state immutability and the between-time distinct successor.
3. Attack the bounded purity grammar with executable counterexamples: mutable metadata/defaults,
   callback or callable donation, imported/class aliasing, module hooks, capture/rebinding forms,
   dynamic calls/attributes, hidden writes, recursive/loop/process-exit paths, implicit protocols,
   opaque factory escape/partial initialization, and cross-role venue-extractor paths.
4. Treat the static scanner and executable runtime seals as one layered proof. A scanner-only
   counterexample is blocking only if it also bypasses the public wrong-type no-access matrix,
   passive exact-type/value graph, dependency provenance, or another owning executable gate.
5. Verify both opaque production-shaped constructors remain feasible, every source-reachable local
   helper and referenced imported binding is authenticated without production execution, and every
   meta-control can fail for its named mutant.
6. Require a concrete reachable bypass, counterexample, contract contradiction, or failure-capable
   missing control for P0/P1. Keep speculative hardening, style, and preference at P2/advisory.

Author evidence to reproduce or independently reconcile:

- focused collection: 290 tests;
- exact RED result: 233 expected failures / 57 passes, comprising 230 deliberate protection-module-
  absence failures plus 3 required inventory/AST/export deltas, with no helper/meta-oracle failure;
- predecessor execution-core corpus excluding the three RED files: 698/698 passed;
- Ruff check/format-check, Python 3.11 grammar parsing, staged/diff check, activation-base scope,
  three accepted ADR digests, eight-file current-source effect scan, and production absence: pass;
- local runtime is Python 3.12.13; actual Python 3.11 execution is unverified locally and remains an
  unchanged exact-head CI obligation.

Do not implement production, edit tests/WO/request, use credentials, call Alpaca, execute SQL/DDL,
initialize a database, alter runtime/persistence, merge, delete, or clean up artifacts. Review is
read-only except for its result.

Write findings only to `work/review/REV-0050/RED-TENTH-RESULT.md`. For each finding include exact
file/line, authority, concrete disproof, and what resolves it. End with `BLOCK`,
`ACCEPT-WITH-CHANGES`, or `ACCEPT`; state unverified items and exact P0/P1 counts. This verdict
governs only permission to begin WO-0148 production implementation. It neither accepts production
nor closes the work order.
