# FINDING — envelope lifecycle/eventing gaps (grouped P2/P3)

- **Status:** OPEN (REV-0022 Phase A; spec-attacker SPEC-05..10 + completeness-critic CC-04/05/06).
- **Severity:** P2/P3 individually; grouped because they share remediation surfaces
  (terminal-state semantics, event provenance, replay). Remediation: WO-0029 (DRAFT — planning
  seat should re-cut; ADR-009 text portions human-gated).
- **Cluster:** F8 in `work/review/REV-0022/phase-a.md`.

| ID | Sev | Gap |
|----|-----|-----|
| SPEC-05 | P2 | Broker-authoritative overfill of the qty ceiling while FROZEN: remaining is **clamped** at 0 (H2 says hard rails are never clamped), no FROZEN→BREACHED edge exists (transitions.py), and resume auto-COMPLETES — a ceiling-violated mandate terminates in the success state with zero `envelope_breached` events. ADR §2 (violation→BREACHED) and §3 (edge set) contradict each other; code silently picked a third option. **ADR amendment required.** |
| SPEC-06 | P2 | Expiry disposition executes AFTER the terminal EXPIRED write, one-shot, best-effort (monitoring.py:701-710); the envelope pass lists ACTIVE only. A failed/crashed CANCEL_AND_RETURN venue cancel is never retried → the "stuck protective LIMIT" ADR-009 claims solved-by-construction can rest forever. Needs a retry/convergence loop (reconcile-driven) + pin. |
| SPEC-07 | P2 | Disposition cancels (stale-data CANCEL, expiry CANCEL_AND_RETURN) emit NO envelope_action event, NO envelope_id provenance — only an uncorrelated `cancel_pending` order event. §6 lists `cancel` in the envelope_action family; `_BUDGET_ACTIONS` (policy.py:64) counts `"cancel"` events that no code path ever writes (dead accounting). Decide: do disposition cancels spend budget? Then emit provenance events accordingly. H10 replay omits these actions until fixed. |
| CC-04 | P2 | The 13 new envelope event types have zero projector/replay coverage: `app/events/` contains no envelope reference, and `verify_dual_store_parity`/readmodel parity exclude the envelope surface entirely — the WO-0016 §6 amendment's stated rationale ("machine not replayable from the log without them") was never implemented or enforced. Envelope dual-store agreement rests solely on write-path tests. |
| CC-05 | P2 | `ExecutionEnvelope.replaces_used` has no writer anywhere in app/ — the cockpit "replaces N/budget" column reads 0 forever, including one action before EXHAUSTED; models.py:674's "(engine seam increments)" comment is FALSE (doc/code conflict on an operator-decision surface, recorded per the conflict rule). Real budget truth lives only in `_replaces_used(events)` which the API/cockpit never computes. |
| SPEC-08 | P3 | TIMEOUT_QUARANTINE does not "pause": the tick's catch-all converts `EnvelopeActionPausedError` into FROZEN reason `policy_error:EnvelopeActionPausedError`; after clean resolution the envelope still demands human resume, mislabeled as a policy error. Handle the pause exception distinctly from policy crashes. |
| SPEC-09 | P3 | ADR-009 §5's "write-time rejection ⇒ validators disagree ⇒ software defect" is falsified by the repo's own pinned chaos test (benign fill between plan and write trips the same tripwire). **ADR text amendment** — the spec conflates validator drift with legitimate state change; operator signal calibration depends on this distinction. (F4 makes the miscalibration acute.) |
| SPEC-10 | P3 | `_hard_rails` accepts a naive `expires_at`; first decide() tick raises TypeError (aware vs naive) and freezes the envelope as policy_error. Fail-closed, but malformed TTL should be rejected at the approval gate (422), not become an approved mandate that dies on tick 1. |
| CC-06 | P3 | Envelope seam Protocols are applied via unchecked `cast(...)` (incl. `cast(Any, store)` at both production executor call sites, monitoring.py:661/677) — mypy verifies nothing at these seams; "mypy green" claims are vacuous there. Folds into the queued interface-lift WO (base.py ABC + facade ABCs); the interim "structural Protocol keeps the seam honestly typed" mitigation is inoperative wherever applied via cast. |
