# REV-0074 R1 independent review result

### [P1] Input-ID persistence cannot replace the authority reducer's alternate-key history

- Location: `work/queue/M2-EXECUTION-2026-08-21/06-WO-0168A-FROZEN-OPERATION-STATE-CONTRACT.md:96`, `work/queue/M2-EXECUTION-2026-08-21/06-WO-0168A-FROZEN-OPERATION-STATE-CONTRACT.md:197`, `work/queue/M2-EXECUTION-2026-08-21/06-WO-0168A-FROZEN-OPERATION-STATE-CONTRACT.md:298`
- Requirement: REV-0074 R1 lenses 2 and 4 require every opaque-state member to have a complete authenticated bounded or direct durable substitute before source activation; WO-0168a FR-2/FR-6 require authentic reducer-state reconstruction rather than a weaker approximation.
- Evidence: `reproduced-live` — repository root and branch matched the request; candidate `6e14c4735cf8803b21948e3df6f7825e6afbcd37` has parent `302a4ed553b62deb51aeb28ad85c497bcd8c2b28` and tree `e34dd03bd61dd7b974a6610b991aa9ca1cf1db7d`; base `0777fab62598f85ce189f40eb1a69319791282c2` has tree `1db6fe831fc7d7785d032c224072b131cd5643e9`; the remediation range contains exactly the three planning changes plus `disposition.md`, the whole-lineage range contains exactly those four files plus the original request/result, and both ranges pass `git diff --check`. HEAD `57d1884b573b4ad3e90225194535a1408c828f07` differs from the candidate only by `request-r1.md`. `static-reasoning` — the frozen operation identity is `command.input_id`, and `durable_input` freezes only that domain/identity plus coordinates/version/payload. The contract nevertheless omits both `_query_by_id` and `_consumed_grant_ids` as if that input-keyed lookup were an equivalent direct substitute. The accepted reducer keys those members independently: `app/execution_core/authority.py:9481` rejects an already retained `query_claim_id`, while `app/execution_core/authority.py:678` and `app/execution_core/authority.py:7699` enforce one-use `EmergencyGrantId`. Two commands can therefore have different `input_id` values while reusing the same query or grant identity, and the frozen five-family schema/repository surface names no direct lookup or uniqueness authority for either alternate key.
- Impact: after restart, an otherwise unseen `AuthorityOperation` can no longer prove that its `query_claim_id` was already claimed, so it can consume budget and emit the same broker-query claim again. The same contract cannot authenticate the consumed-grant latch if grant issuance later restores an identical grant. An implementation that scans canonical payload history, one that silently adds alternate-key columns/indexes, and one that accepts the duplicate would all fit the current text, with materially different behavior and boundedness. The original P1 is therefore not completely corrected.
- Resolution: freeze direct durable authority for `query_claim_id` and consumed `EmergencyGrantId` (exact columns/table family, uniqueness and immutability rules, record/repository methods, hydration proof, row-write placement, and restart/alternate-input-ID mutants), or retain an equally exact bounded representation that preserves the reducer's one-use semantics. Reconcile the five-family and exact-path claims and submit the amended head for fresh preflight.

Verdict: ACCEPT-WITH-CHANGES
P0: 0
P1: 1
P2: 0
Unverified: Full body-level disproof of every remaining operation payload and every other state-family member; canonical-byte known-answer completeness; detailed DDL trigger/FK sufficiency; executable runtime or SQLite behavior, which the packet prohibits. Review stopped at the user's instruction after the decisive P1 was established.
