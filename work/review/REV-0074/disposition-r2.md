# REV-0074 R2 author disposition

`result-r2.md` is accepted as valid negative evidence and remains unchanged.

The R2 finding is corrected by an exact semantic-key grammar, fixed kind octets, kind-specific
coordinate and source arrays, eight known-answer payload/digest pins, exact table columns, partial
unique indexes, ordered record members, and public codec names. Venue keys deliberately span
application generations/sessions within an execution profile; authority one-use keys bind exact
application generation, execution profile, and scope. Conditional insertion remains owner-driven,
so a refusal cannot consume a key.

No source or SQLite is activated. `request-r3.md` binds the amended candidate for a fresh result.
