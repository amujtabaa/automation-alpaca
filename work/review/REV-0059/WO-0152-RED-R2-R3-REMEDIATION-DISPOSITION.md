# WO-0152 R2-R3 static-exception consistency disposition

Status: DRAFT CORRECTION - NOT ACTIVE  
Date: 2026-08-07  
Work order: WO-0152  
Packet: REV-0059

## Retained R2-R2 candidate

The R2-R2 candidate is retained unaccepted evidence. Before an independent
verdict, the author found two internal static-table contradictions:

1. its general prohibition accidentally rejected inherited exact operations in
   the environment, approved-mandate, and terminal-parent fixtures; and
2. it required `SeenFactIndex.observation_at` to use a property replacement,
   although the public interface is a keyed method.

`result-r2-r2.md` remains absent. R2-R2 is neither accepted nor rejected on
test semantics; it is retained solely to make the stop and replacement
visible.

A source-level cross-seat adjudication also confirmed that
`VenueRecoveryBook.effect` remains in the boundedness tripwire: despite its
keyed lookup, its public implementation materializes retained contradiction
history. The exact tripwire count remains sixteen; R2-R3 changes its trap
shapes and exception-table consistency only.

## Exact R2-R3 correction

Under the user's existing in-flight issue-resolution authority, R2-R3:

1. changes active current wording so that only exact independent R2-R3
   `ACCEPT` at P0=0/P1=0 may activate E3; and
2. replaces the contradictory general static rule with one coherent four-helper
   table, preserving every inherited lexical limit and specifying fourteen
   property-shaped plus two method-shaped public tripwire replacements.

R2-R3 adds no new fixture, source/test implementation, public API, private
production capability, runtime, database, SQL/DDL, broker/network, credential,
CI, commit, push, merge, deletion, cleanup, force-push, or rebase authority.
It preserves all R2/R2-R1 sibling-history, fixture, terminal-certification,
paired E2/E3 93% closeout, and safety boundaries.

## Stop rule

WO-0152 remains DRAFT. No E3 test module may be created, run, or accepted until
the exact R2-R3 manifest independently returns `ACCEPT` with P0=0/P1=0. Any
P0/P1 requires the smallest complete root correction or an explicit new human
boundary.
