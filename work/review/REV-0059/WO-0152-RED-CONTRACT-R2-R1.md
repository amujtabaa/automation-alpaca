# WO-0152 E3 RED contract R2-R1 - activation-gate correction

Status: REPLACEMENT CANDIDATE - DRAFT ONLY - NOT ACTIVE  
Date: 2026-08-07  
Work order: WO-0152  
Packet: REV-0059  
Controlling composite: WO-0152-RED-CONTRACT-R1.md,
WO-0152-RED-CONTRACT-R1-R1.md, WO-0152-RED-CONTRACT-R2.md, and this R2-R1

## 1. Retained R2 candidate and narrow replacement

The R2 candidate is retained unaccepted evidence. Its independent preflight
was stopped before a verdict because the work order's future activation gate
incorrectly named the superseded R1 acceptance. R2-R1 corrects only that
current work-order/lifecycle inconsistency. It does not alter any R2 semantic
or static-control requirement.

Every R2 clause remains verbatim controlling, including the exact six-step
public same-account OTHER-symbol lifecycle, pre-install guards, one
copied-authority literal `venue` installation, post-install pure public target
bootstrap assertion, fixture limits, static negative controls, and all safety
exclusions.

## 2. Replacement activation rule

The only activation condition for this E3 composite is all of the following:

1. the accepted WO-0151 E2 implementation and retained exact-head #741
   functional/static success with its 91.34% coverage-only failure;
2. this exact R2-R1 composite frozen in an immutable manifest; and
3. a fresh independent review of that exact R2-R1 manifest returning `ACCEPT`
   with P0=0/P1=0.

No R0, R1, R1 remediation 01, or first R2 result can substitute for item 3.
The prior R2 candidate has no result and cannot activate WO-0152.

The work order must name the R2-R1 packet/disposition/manifest/request/result
paths before activation. The work order remains DRAFT and `NOT_GRANTED` until
the exact R2-R1 acceptance rule is satisfied.

## 3. Preserved closeout and scope boundaries

An R2-R1 `ACCEPT` permits only the already authorized test-only E3 activation
and implementation within the work order's exact allowed paths. It does not
close WO-0151, close WO-0152, declare M1 complete, waive the unchanged paired
E2/E3 93% exact-head Python 3.11/3.12 gate, or grant a coverage exception.

No production code/API, database/SQL/DDL, runtime wiring, broker/network,
credentials, CI-workflow change, M2, master merge, PR, deletion, cleanup,
force-push, or rebase is authorized.

## 4. Acceptance and stop rule

The independent reviewer must re-derive the R2-R1 replacement against the
user authorization, retained packet chain, accepted ADRs, current work order,
ratification/provenance, and frozen source. It must verify the first R2
candidate was stopped without a result, the future E3 test path remains absent,
and no source/test implementation occurred. It must verify that R2-R1 does not
weaken the root-correct R2 restriction while replacing the stale activation
reference completely.

Only exact independent `ACCEPT` with P0=0/P1=0 permits WO-0152 activation. Any
P0/P1 keeps it DRAFT and requires the smallest root correction or a new human
boundary.
