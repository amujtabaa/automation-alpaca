# Spine v2 Migration Matrix

This matrix tracks whether a behavior remains legacy truth, is shadow-evented, or has migrated to event-log truth.

Status values:

- `legacy_truth`: existing tables/store paths remain authoritative.
- `shadow_evented`: events are emitted for comparison, but legacy path remains authoritative.
- `event_truth`: first durable write is an `ExecutionEvent`; tables are projections/read models.
- `blocked`: do not implement until an upstream decision or harness exists.

| Area | Current status | Target status | Notes |
|---|---:|---:|---|
| Broker-authoritative fill ingestion | `event_truth` (P3 wave 3a-truth) | `event_truth` | ✅ Decision 1. **Wave 3a-shadow** emits a broker-authoritative `FILL` `ExecutionEvent` atomically with each fill; **wave 3a-truth** flipped position derivation to fold that event log (`project_symbol_position`) — the fill table is now a compatibility read-model, backfilled at init for pre-wave-3a rows. Truth proven: a FILL event with no fill row moves position (`tests/test_spine_phase3_fill_event_truth.py`); whole position/fill corpus green (characterization). |
| Fill deduplication | `event_truth` (P3 wave 3a) | `event_truth` | ✅ Decision 1 / INV-5. `FILL` events carry a composite `fill:{order_id}:{source_fill_id}` dedupe_key mirroring the fill table's per-order dedup exactly; the event log now backs position, so dedup is enforced through the event log. |
| Overfill/negative-position handling | `event_truth` (P3 wave 3b) | `event_truth` | ✅ ADR-001. **Wave 3b** flips a broker-authoritative overfill (a SELL crossing long-only through flat) from reject-and-drop to RECORD + QUARANTINE: `plan_append_fill` appends the fill row + a `fill_overfill_quarantined` audit event + the broker `FILL` `ExecutionEvent` atomically; the projector records the short (`apply_fill(allow_short=True)`), `list_quarantined_symbols` derives quarantine from the event log, and `create_order_for_candidate` blocks autonomous BUY intent for a quarantined symbol (`order_intent_blocked_quarantine`). Malformed *local* input (non-positive qty/price, cumulative-over-order, symbol/side mismatch) still rejects. Replay reproduces the quarantine per-store + across memory/SQLite; characterization + parity tests migrated (reject→record). |
| Timeout/504 submit ambiguity | `event_truth` (P3 wave 3c) | `event_truth` | ✅ ADR-002. **Wave 3c** replaces blind redrive of an ambiguous submit (timeout/504/transport) with `OrderStatus.TIMEOUT_QUARANTINE`, whose first durable write is a `TIMEOUT_QUARANTINE` `ExecutionEvent` (the order-row status a co-written read-model; quarantine set derived via `timeout_quarantined_order_ids`, replay-stable + dual-store-parity). The adapter classifies (`AmbiguousBrokerError`); monitoring routes it to quarantine (both submit choke points) and resolves it with a READ-ONLY targeted `get_order_by_client_order_id` query (`_resolve_timeout_quarantine`) → SUBMITTED (working/filled, fills ingested via SUBMITTED — INV-9) / CANCELED / REJECTED (bounded confirmed-absent) / manual-review (persistent inconclusive). Never blind-resubmits (double-fire safe); the quarantined order is structurally unreachable by any resubmit sweep and is refused a manual cancel. Scoped to the quarantine fact (spawn projector deferred to Phase 4 — conflict C5). See `docs/SPINE_WAVE3C_PLAN.md`. |
| Atomic submit claim | `legacy_truth` | `event_truth` | Salvage prior claim semantics inside single-writer engine. |
| Manual flatten | `event_truth` (P3 wave 3e) | `event_truth` | ✅ ADR-003 / Decision 3 (E1 ruled **Option B**). **Wave 3e** denies an ordinary manual flatten by default while `Halted` (`FlattenBlockedError` → 409) and keeps it allowed in `Active`/`Reducing` — the kill switch is now a true all-stop, reversing the legacy D-P2 "flatten always works even kill-switched". The deny decision reads the `event_truth` §8 `TradingState` (wave 3d) + the emergency-override event log, both under the flatten's lock; gating is at *creation* (`plan_flatten_position`), since `flatten_position` is the sole `MANUAL_FLATTEN` producer, so the submission/claim gate is untouched (an already-authorized in-flight exit completes). Scope note (mirror 3c-C5): the `MANUAL_FLATTEN` order/sell-intent rows stay legacy read-models (order/spawn projector is Phase 4); the *facts* flipped are the allow/deny decision (via `trading_state`) + the override grant/consume. Characterization Flow-1 migrated (reject→deny); pinning tests consciously broken + re-pinned (`tests/test_spine_phase3e_manual_flatten.py`). The **facade migration** of the flatten route (ADR-005 / E6) is deferred to the "API routes" row. |
| Emergency reduce override | `event_truth` (P3 wave 3e) | `event_truth` | ✅ ADR-003 / Decision 3. **Wave 3e** adds the audited operator override that scopes a SINGLE reduce-only exit while `Halted`: `EMERGENCY_REDUCE_OVERRIDE`/`_RESOLVED` `ExecutionEvent`s (durable truth), a pure `active_emergency_reduce_overrides` projector (latest-wins per `{session, symbol}`), and `authorize_emergency_reduce_override` which atomically enforces the ADR-003 preconditions — session `Halted`, open position, and **no unresolved `TIMEOUT_QUARANTINE` order for the symbol (INV-3)** — then grants the override. `POST /api/positions/{symbol}/emergency-reduce` authorizes, stands down buys, and runs the reduce-only flatten, which sees the grant, creates the exit, and **consumes** the override in the same lock hold (single-use). E3: the global `TradingState` stays `Halted` throughout — the grant is a separate scoped authority, NOT a global state flip (kill dominates). Replay reproduces the grant/consume lifecycle; dual-store parity. E5: the actor is a placeholder pending a real auth gate ("Auth for command endpoints" row). See `docs/SPINE_WAVE3E_PLAN.md`. |
| Kill / TradingState | `event_truth` (P3 wave 3d) | `event_truth` | ✅ Spine v2 §8. **Wave 3d** introduces the three-state `TradingState` FSM (`Active` / `Reducing` / `Halted`). Today the two legacy booleans are its only driver (kill → `Halted`; else pause → `Reducing`; else `Active`; kill dominates), but `trading_state` is a deliberately **independent** field — §8 makes `Reducing` "the default under stream degradation or pending reconciliation", so Phase 4 will drive it from stream/reconcile signals WITHOUT touching the booleans (a validator/@property forcing `trading_state == of(kill, pause)` would silently heal that away — reviewed-out in wave 3d). Each `set_kill_switch`/`set_buys_paused` first-writes a `TRADING_STATE_CHANGED` `ExecutionEvent` whose `to` is the durable FSM truth; `current_trading_state` folds that log (latest-wins, session-scoped, replay-stable + dual-store parity) and the `SessionRecord.trading_state` column is a co-written read-model reconstructable from it. The event also stamps the `(kill, pause)` tuple as context, but the two booleans are NOT purely event-reconstructable (no event fires when a boolean toggle leaves the derived state unchanged); they stay co-written `sessions`-table columns, and independent-release (pause surviving a kill-release) is durable via those columns. The three Rule-8 predicates (`order_intent_block_reason`/`session_submission_block_reason`/`kill_switch_block_reason`), the protection-pause enforcement (`monitoring.py`), and the protection-status DTO all READ the FSM field (reason strings `kill_switch`/`buys_paused` kept for label continuity); divergent-session tests lock that the read follows `trading_state`, not the booleans. Graded `Reducing`-is-reduce-only semantics (INV-7 / ADR-003) pinned in `tests/test_spine_phase3d_trading_state.py`; pre-wave-3d DBs backfilled at init (raw column healed). See `docs/SPINE_WAVE3D_PLAN.md`. |
| API routes | 3 facade-backed (P1) | facade-backed | Decision 5. **Phase 1** migrated `GET /positions` + pause/resume-buys behind the Execution{Command,Query}Facade; remaining routes still `legacy_truth`. |
| Streamlit cockpit | likely thin | API-client-only | Verify imports; enforce boundary later. |
| Alpaca adapter | concrete adapter | adapter-only SDK import | Add outcome classifier/token bucket/stream handling later. |
| Reconciliation | `event_truth` (P4 waves 4e–4h; runtime + startup + reconnect, all §7 goals met) | `event_truth` | Startup mass reconcile + targeted query + unmanaged order surfacing. **Waves 4a/4b/4c** landed the adapter mass reports + pure `plan_reconciliation` engine + synthetic-fill append. **Wave 4d** ran the engine each tick as a SHADOW (superseded). **Wave 4e** turned it into the ACTING reconcile (`reconciliation_enabled`, default on): **4e-1** §7 config + query budget; **4e-2** external/unmanaged order surfacing (deduped audit records, §7 "never absorbed"); **4e-3** the oversell-critical not-found→terminal resolution — a mass-report *absence* is never a reject on its own; a READ-ONLY targeted `client_order_id` query + `open_check_missing_retries` gate every `SUBMITTED→REJECTED`/`PARTIALLY_FILLED→CANCELED` (event-authoritative via `reconcile_resolve_order`, BROKER_AUTHORITATIVE; FILLED refused, INV-9); **4e-4** reconciliation-inferred synthetic fills (`SYNTHETIC`/`RECONCILIATION`, dedup-safe INV-5/R8) + the wired `ReconcileQueryBudget` throttle (skip-never-flat); **4e-5** flipped this row to event-truth: the reconcile facts' first durable write is an `ExecutionEvent` (REJECTED/CANCELED lifecycle event; synthetic FILL event), position replays from the log, dual-store parity holds (order-status column is a co-written read-model — order-status projector deferred, mirror of 3c-C5). **Wave 4f** adds the startup mass-reconcile GATE (§7 "trading not enabled until reconcile"): `run_startup_reconcile` enters reduce-only (`Reducing`) via a SECOND independent `TradingState` driver — composed with the control driver via `compose_trading_state` (Halted>Reducing>Active; kill dominates; R2) — lifts to `Active` only on confirmed parity, and on a reconcile FAILURE stays `Reducing`, NEVER auto-`Halted` (R3: a held position stays exitable at boot). **Wave 4g** wires reconnect→reduce-only+reconcile (`on_stream_reconnect`; R1 sim-seam, no real trade-update stream yet). **Wave 4h** adds broker-vs-local position-parity surfacing (durable, deduped `reconcile_position_mismatch` needs-review record — position truth NEVER overwritten, Rule 7; holds reduce-only until cleared) + the operator read route `GET /api/reconciliation` (ADR-005 facade-backed: `list_external_orders`/`list_position_mismatches` map durable audit records to typed DTOs; api→facade dep direction, Phase-5-clean). Surfacing + the FSM drive are gated behind `drive_state` (loop/startup/reconnect only) so the direct-tick corpus stays inert. **Remaining follow-ups (do NOT gate this row):** the order-status/spawn projector (Phase 4→spine §4, mirror of 3c-C5); real trade-update-stream wiring to replace the 4g sim-seam. |
| Event log | shadow (P2) | durable truth | Decision 4. **Phase 2 (shadow):** `ExecutionEvent` schema + monotonic sequence + `dedupe_key` idempotency + dual-store append/query + pure `PositionProjector` + replay verifier landed (`app/events/`, `app/models.py`). Persisted-snapshot tables + `event_truth` flip are Phase 3. |
| In-memory/SQLite parity | replay verifier (P2 position + P6-B read-models) | replay verifier | **Phase 2** added the dual-store event-log projection parity verifier (`app/events/replay.py`), extending the prior fill-table parity discipline to the event log. **P6-B (legacy-table demotion)** extends it beyond position to the full event-truth read-model surface: `project_read_models` + `verify_dual_store_readmodel_parity` fold the overfill-quarantine set (3b), timeout-quarantine set (3c), per-session `TradingState` (3d/4f), and emergency-reduce overrides (3e) — each a co-written read-model column — from either store's log and assert they reconstruct identically (`tests/test_phase6b_readmodel_parity.py`, incl. a from-scratch-replay == store-fold reconstruction proof + a divergence-detection negative test). This proves the demoted columns are event-reconstructable, which is the point of Phase 6 table demotion. **Deferred (documented, does NOT gate Phase 6):** a full order-status/spawn state-machine projection — Spine §4 primary/spawn phase (mirror of 3c-C5); the order-row `status` column stays a co-written read-model whose safety-critical derived quantity, position, IS projected + parity-checked. |
| Import-linter | `enforced` (P5) | `enforced` | ✅ ADR-006. **Phase 5** enabled import-linter (`.importlinter`, CI `lint-imports` step + `tests/test_import_boundaries.py`). Four Tier-1 hard-invariant contracts hold with zero exceptions: alpaca-SDK confined to the two concrete ports (inv #5), cockpit imports no `app.*` (inv #4), engine venue-agnostic (imports only the abstract ports), models is a leaf. A fifth Tier-2 contract enforces the ADR-005 "routes reach the backend only via the facade" TARGET as a **ratchet** — its `ignore_imports` block is the exhaustive Phase-6 punch-list of the 17 remaining direct route→backend edges (store/broker/monitoring AND engine-internal policy/approval/features); `unmatched_ignore_imports_alerting = error` means a migrated route's stale ignore errors until deleted, so the boundary only tightens. The four Tier-1 runtime-safety invariants are additionally re-proven directly against the grimp graph (INI-independent). **Review remediation:** the credential-safe adapter/feed factories were lifted out of the package `__init__` into `app.broker.factory` / `app.marketdata.factory` so the bare packages (and the abstract port re-exported from them) no longer transitively reach `alpaca` — closing the idiomatic `from app.broker import BrokerAdapter` leak the reviewer found. |
| Auth for command endpoints | absent/limited | required | Shared token/JWT minimum for commands/kill/emergency. |

## Migration rule

Do not mark a flow `event_truth` until:

1. the first durable write is an `ExecutionEvent`;
2. replay reproduces the live projection;
3. in-memory and SQLite projections agree;
4. characterization tests capture old behavior where relevant;
5. accepted ADR behavior is tested;
6. API routes no longer mutate legacy state directly for that flow.
